version: '3.7'
services:
  dht:
    build: .
    restart: always
    devices:
      - "/dev/mem"
    networks:
      - isengard

  sht:
    build: .
    #restart: always
    devices:
      - "/dev/mem"
    networks:
      - isengard

networks:
  isengard:
    external: True
